{{int,int}}/字符串分割

    greedy
        t452    气球题
                排序后, 有overlap的 range, 把多余的进行切割, 实现最小的范围交集

        t435    和t452区别
                排序后, 他不是对overlap进行切割, 而是把范围大的 那个范围直接删除

        t763    分割依据 每个字符只能存在一个字符串里,
                例如 babacacb dfgd hsh f

合并
        t56     区分不同集合
                和t452 最像
                和t436 像
                需要进行排序, 但是 他不用进行什么切割, 他就是 取最大的范围, 就是并集
