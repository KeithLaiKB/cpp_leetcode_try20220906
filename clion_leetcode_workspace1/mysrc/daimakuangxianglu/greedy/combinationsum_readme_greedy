t455
    局部最优:
        先让胃口小的 吃到 最小且合适 的饼干


t376:
    没感觉到


t53
    局部最优:
        让 大的 total_sum值
        并且 total_sum值一旦小于0,
            重新计算下一个 total_sum值,
                不然  负数的 max_total_sum值 会影响下一个数


t55
    局部最优:
        以往能到达的最远处idx vs 当前能到达的最远处idx
        选取 能够到达 最远处idx 的方案


t45
    局部最优:
        以往能到达的最远处idx vs 当前能到达的最远处idx
        选取 能够到达 最远处idx 的方案
        但是
            只有 i==以往能到达的最远处idx
            才进行step+1

